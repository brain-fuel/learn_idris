module Main

import System

%default total

-- Exercise 03: sleep
--
-- `sleep : Nat -> IO ()` blocks the current process for the given
-- number of seconds. It lives in `System` (not `System.Clock`).
--
-- TODO: print "starting", call `sleep 1`, then print "done".

main : IO ()
main = putStrLn "FIXME"

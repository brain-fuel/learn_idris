package ch15-google-sheets
version = 0.0.1
authors = "brainfuel"

-- depends on pack `http`. JSON serialization uses contrib `Language.JSON`
-- (auto-shipped with idris2), so no pack `json` dep needed.
depends = http

sourcedir = "."
modules =

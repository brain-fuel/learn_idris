module Generated.Routes

import JSON.Derive
import Shared.Ids

%default total
%language ElabReflection

-- This file is REGENERATED by web/content-build at pre-build time.
-- The stub below is a placeholder so the dependency graph compiles before
-- the content-build executable runs for the first time. Real output will
-- enumerate one Route constructor per chNN-slug directory and provide
-- lessonMeta / allRoutes / fromSlug / toSlug.

public export
data Route : Type where
  RStub : Route

public export
record LessonMeta where
  constructor MkLessonMeta
  chapter   : Nat
  slug      : LessonSlug
  title     : String
  assetPath : String

%runElab derive "Route"      [Show, Eq, ToJSON, FromJSON]
%runElab derive "LessonMeta" [Show, Eq, ToJSON, FromJSON]

public export
lessonMeta : Route -> LessonMeta
lessonMeta RStub = MkLessonMeta 0 (MkLessonSlug "stub") "Stub" "/assets/stub.md"

public export
allRoutes : List Route
allRoutes = [RStub]

public export
toSlug : Route -> LessonSlug
toSlug RStub = MkLessonSlug "stub"

public export
fromSlug : LessonSlug -> Maybe Route
fromSlug (MkLessonSlug "stub") = Just RStub
fromSlug _                     = Nothing
